<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mentor Profiles</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <header>
        <nav>
            <ul>
                <li><img src="/media/logo.jpg" alt="Institute Logo" /></li>
                <li><a href="/">Home</a></li>
                <li><a href="/about">About Us</a></li>
                <li><a href="/mentorship_oppur">Mentorship Opportunities</a></li>
                <li><a href="/mentorprofiles">Mentor Profiles</a></li>
                <li><a href="/resources">Resources</a></li>
            </ul>
        </nav>
    </header>
    <h1>This is the Mentor Profile Page</h1>
    <section id="mentor-profiles">
        <h2>Mentor Profiles</h2>
        <!-- Mentor profiles dynamically generated from the server data -->
        <div class="mentor">
            <% if (data && data.length > 0) { %>
                <% data.forEach((mentor) => { %>
                    <div class="mentor-profile">
                        <h3><%= mentor.username %></h3>
                        <p><strong>Category:</strong> <%= mentor.category %></p>
                        <!-- Add other profile details as needed -->
        
                        <!-- Example: Displaying an image if the mentor has a profile picture
                        <% if (mentor.profilePicture) { %>
                            <img src="<%= mentor.profilePicture %>" alt="Profile Picture">
                        <% } %> -->
                    </div>
                <% }); %>
            <% } else { %>
                <p>No mentor profiles available.</p>
            <% } %>
        </div>
    </section>

    <!-- Debugging: Output the 'data' variable -->
    <!-- <script>
        console.log('Data:', <% = data)%>);
    </script> -->
</body>
</html>
